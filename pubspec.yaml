name: shorebird_playgrounds
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Utils
  restart_app: ^1.2.1

  # Code Push
  shorebird_code_push: ^1.1.2

  # UI
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Linter
  very_good_analysis: ^5.1.0

flutter:
  assets:
    - shorebird.yaml
  uses-material-design: true
